version: "0.1.0"

dependencies:
  idf: ">=5.1"

# Use LOCAL copies (no registry downloads)
espresif/esp-mesh-lite:
  override_path: ../components/mesh_lite

espresif/wifi_provisioning:
  override_path: ../components/wifi_provisioning
