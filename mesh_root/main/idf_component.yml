# mesh_root/main/idf_component.yml
version: "0.1.0"

dependencies:
  idf: ">=5.1"

  espressif/esp-mesh-lite:
    override_path: ../../components/esp_mesh_lite   # <-- go up two

  espressif/esp-iot-bridge:
    override_path: ../../components/esp_bridge      # <-- go up two
