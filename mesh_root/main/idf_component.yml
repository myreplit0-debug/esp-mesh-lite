# mesh_root/main/idf_component.yml
version: "0.1.0"

dependencies:
  idf: ">=5.1"

  # Use LOCAL copies (no registry downloads)
  espressif/esp-mesh-lite:
    override_path: ../components/esp_mesh_lite

  espressif/esp-iot-bridge:
    override_path: ../components/esp_bridge
